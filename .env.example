# URL base do Autonomous (a mesma do Database Actions, sem o /ords/...)
# ex.: https://abc123xyz-rotasdb.adb.sa-saopaulo-1.oraclecloudapps.com
ORDS_BASE_URL=

# alias REST do schema (o que aparece em /ords/<isso>/ — em minúsculo)
DB_SCHEMA_PATH=acesso_app

# usuário e senha do schema no banco
DB_USER=ACESSO_APP
DB_PASSWORD=
